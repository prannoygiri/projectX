
type Employee {
  id: Int
  name: String
  email: String
  dob: Date
  doj: Date
  created_at: String
  updated_at: String
  tasks: [Task]
}

type Query {
  employee(id: Int!): Employee
  employees: [Employee]
}
