
type Employee {
  id: Int!
  name: String!
  age: Int!
  sex: String!
  created_at: String
  updated_at: String
  tasks: [Task]
}

type Query {
  employee(id: Int!): Employee
  employees: [Employee]
}
